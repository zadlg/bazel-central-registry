matrix:
  platform: ["centos7", "debian10", "macos", "ubuntu2004"]

tasks:
  verify_targets:
    name: "Verify build targets"
    platform: ${{ platform }}
    build_targets:
    - '@tree-sitter//:tree-sitter'
